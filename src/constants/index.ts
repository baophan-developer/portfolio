export enum NameLinkIds {
    Homagpage = "Homagpage",
    Services = "Services",
    Portfolio = "Portfolio",
    Contact = "Contact",
    About = "About",
}

export const NameLinks = Object.values(NameLinkIds) as string[];

export enum ParallaxType {
    Introduce = "introduce",
    SkillTool = "SkillTool",
    Education = "education",
    Experience = "experience",
}
